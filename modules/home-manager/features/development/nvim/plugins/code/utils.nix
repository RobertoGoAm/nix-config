{
  plugins = {
    colorizer = {
      enable = true;
    };

    emmet = {
      enable = true;
    };

    multicursors = {
      enable = true;
    };

    nvim-autopairs = {
      enable = true;
    };

    rainbow-delimiters = {
      enable = true;
    };

    refactoring = {
      enable = true;
    };

    ts-autotag = {
      enable = true;
    };

    vim-surround = {
      enable = true;
    };
  };
}
