{
  plugins = {
    colorizer = {
      enable = true;
    };

    emmet = {
      enable = true;
      settings = {
        leader_key = ",";
        settings = {
          javascript = {
            extends = "jsx";
          };
          typescript = {
            extends = "jsx";
          };
        };
      };
    };

    multicursors = {
      enable = true;
    };

    nvim-autopairs = {
      enable = true;
    };

    rainbow-delimiters = {
      enable = true;
    };

    refactoring = {
      enable = true;
    };

    ts-autotag = {
      enable = true;
    };

    vim-surround = {
      enable = true;
    };
  };
}
