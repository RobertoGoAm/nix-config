{
  plugins = {
    auto-session = {
      enable = true;
    };

    firenvim = {
      enable = true;
    };

    hardtime = {
      enable = true;

      settings = {
        disable_mouse = false;
        enable = true;
        restriction_mode = "hint";
      };
    };

    intellitab = {
      enable = true;
    };

    repeat = {
      enable = true;
    };
  };
}
