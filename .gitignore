# Secrets
secrets/
*.age
*.sops.yaml
*.sops.json
private/
*.env

# Nix
# `nix build` / darwin-rebuild leave these symlinks into the store in the
# working tree; they are build output, not config.
result
result-*

# macOS
.DS_Store

# Serena (MCP) project cache
.serena/